#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    int arr[4];
    // int max = INT_MIN;
    int max_no;
    for (int i = 0; i < 4; i++)
    {
        cin >> arr[i];
    }

    return 0;
}